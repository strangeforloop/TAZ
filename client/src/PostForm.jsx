// Form for submitting a new Give or Take entry.
// A toggle switches between posting to /api/skills (Give) or /api/needs (Take).
// Take entries have an additional "urgent" checkbox.

export default function PostForm() {
  return null;
}
