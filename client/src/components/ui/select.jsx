// Stub — replace with real brutx Select component via: npx brutx@latest add select
// Dropdown used in PostForm for choosing a category (medical, food, tools, labor, other).

export function Select() {
  return null;
}
